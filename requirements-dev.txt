# Copy the content from the requirements-dev.txt artifact I provided above
# Development dependencies for edss-hx711 Viam module
# This includes production dependencies + testing/development tools

# Include all production dependencies
-r requirements.txt

# Core testing framework
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Optional but recommended development tools
black>=22.0.0          # Code formatting
flake8>=5.0.0          # Code linting  
mypy>=1.0.0            # Type checking

# Optional: parallel test execution
pytest-xdist>=3.0.0    # Run tests in parallel with -n auto