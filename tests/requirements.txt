# Test dependencies for HX711 Loadcell component (Raspberry Pi compatible)
# Core testing framework
pytest>=7.0.0,<8.0.0  # Pin to stable version for Pi compatibility
pytest-asyncio>=0.21.0,<1.0.0
pytest-cov>=4.0.0,<5.0.0
pytest-mock>=3.10.0,<4.0.0
pytest-xdist>=3.0.0,<4.0.0  # For parallel test execution

# Mocking and testing utilities (built into Python 3.11+)
unittest-mock>=1.0.0; python_version < "3.11"

# Code quality tools (lightweight versions for Pi)
black>=22.0.0,<24.0.0  # Pin to avoid potential compatibility issues
flake8>=5.0.0,<7.0.0
mypy>=1.0.0,<2.0.0

# Performance testing (optional, can be resource intensive on Pi)
pytest-benchmark>=4.0.0,<5.0.0

# Coverage reporting
coverage>=7.0.0,<8.0.0

# Raspberry Pi specific considerations:
# - These versions are tested on Raspberry Pi OS with Python 3.11
# - Some packages may take longer to install on Pi due to ARM compilation
# - Consider using --no-cache-dir flag with pip on Pi for stability
